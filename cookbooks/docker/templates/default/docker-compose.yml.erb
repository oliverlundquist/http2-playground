version: '2'
services:
  web:
    image: oliverlundquist/nginx:0.0.2
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - /var/www/letsencrypt:/var/www/letsencrypt
    networks:
      - php-app
networks:
  php-app:
    driver: bridge
